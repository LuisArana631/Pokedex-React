export const types = {
    // Tipos para manejar los pokemons
    pokemonList: '[pokemon] List pokemons',
    // Tipos para manejar la interfaz
    loadingGrid: '[ui] Loading Grid',
    endLoadingGrid: '[ui] End loading Grid'
}